#include "UnionFindSetBuildTree.h"

Tree UnionFindSetBuildTree::build_tree(const Flute::Tree &flute_tree,
                                       int root_index) {
  std::cout << "the center of mass and flute tree root node have the same "
               "coordination"
            << std::endl;
  return nullptr;
}